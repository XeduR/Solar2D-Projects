display.setStatusBar( display.HiddenStatusBar )
local composer = require( "composer" )
local sfx = require( "scripts.sfx" )

composer.gotoScene( "scripts.menu" ) -- for playing the actual game
-- composer.gotoScene( "scripts.editor" ) -- for creating new levels
